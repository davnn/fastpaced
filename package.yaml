name:                curiousobserver
version:             0.1.0.0
github:              "davnn/curiousobserver"
license:             BSD3
author:              "David Muhr"
maintainer:          "muhrdavid+github@gmail.com"
copyright:           "David Muhr"

extra-source-files:
- README.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on Github at <https://github.com/davnn/curiousobserver#readme>

dependencies:
- base >= 4.7 && < 5
- hakyll
- hakyll-sass
- pandoc
- filepath

executables:
  curiousobserver:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
